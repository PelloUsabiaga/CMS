@{
    ViewBag.Title = "Login";
}

<div class="row">
    <form id="frm">
        <div>
            <p>
                <div>
                    Email<br />
                    <input type="text" name="Email" />
                </div>
            </p>
            <p>
                <div>
                    Password<br />
                    <input type="password" name="Password" />
                </div>
            </p>
            <p>
                <div><input type="submit" class="btn btn-outline-dark" value="enter" /></div>
            </p>
        </div>
    </form>
</div>


@section scripts{ 

<script>
    $(document).ready(function () {
        $("#frm").submit(function (e) {
            e.preventDefault();

            url = "@Url.Content("~/Access/LoginSend")"
            parametros = $(this).serialize();

            $.post(url, parametros, function (data) {
                if (data == "1") {
                    document.location.href = "@Url.Content("~/Home")";
                } else {
                    alert(data)
                }
            })
        })

    })
</script>
}
